import 'package:flutter/material.dart';

import '../../models/exercise_model.dart';

/// Log progress bottom sheet — implemented in task 3.6.
class LogProgressSheet {
  LogProgressSheet._();

  static Future<void> show(BuildContext context, {required ExerciseModel exercise}) async {
    // TODO(task-3.6): implement number input, unit selector, and save action
  }
}
